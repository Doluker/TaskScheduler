# 📋 TaskScheduler: Планировщик Задач
## Настольное приложение на WPF

Приложение предназначено для эффективного управления личными и рабочими задачами с поддержкой повторяющихся событий и системы приоритетов.

---

### 💻 Технологический стек

* **Платформа:** .NET 9.0 (WPF)
* **Язык:** C#
* **База данных:** PostgreSQL
* **ORM:** Entity Framework Core

### ✨ Основные возможности

* Аутентификация и регистрация пользователей.
* Создание, просмотр и редактирование задач.
* **Автоматическое управление расписанием** (создание повторяющихся задач).
* Фильтрация задач по статусу и категории.

---

### ⚙️ Инструкция по установке и запуску

Для запуска приложения необходимо установить и настроить сервер базы данных PostgreSQL.

#### Шаг 1: Установка зависимостей

1.  Установите и запустите **PostgreSQL** (версия 12+).
2.  Установите **.NET SDK 9.0**.
3.  Убедитесь, что установлен глобальный инструмент EF Core CLI:
    ```bash
    dotnet tool install --global dotnet-ef
    ```

#### Шаг 2: Настройка подключения

1.  **Проверьте/Обновите подключение:** Откройте файл **`DataBaseContext.cs`** в папке **`Models`** и в методе `OnConfiguring` найдите строку подключения. Обязательно измените параметры (`Username`, `Password`, `Port`) на те, которые вы используете на своем сервере:
    ```csharp
    optionsBuilder.UseNpgsql("Host=localhost;Database=TaskScheduler;Username=postgres;Password=your_password;Port=5432");
    ```

#### Шаг 3: Создание структуры таблиц

Миграции (описание структуры БД) уже добавлены в проект. Вам нужно только применить их.

1.  Откройте **терминал**.
2.  Выполните команды для создания всех таблиц в вашей PostgreSQL БД:
    ```bash
    cd TaskScheduler
    dotnet ef database update
    ```
    *Последняя команда подключится к БД и создаст все таблицы (Users, Tasks, Schedules, и т.д.).*

#### Шаг 4: Сборка и запуск приложения

1.  Соберите проект:
2.  Запустите приложение:
