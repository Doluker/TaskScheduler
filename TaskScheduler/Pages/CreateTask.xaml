<Page x:Class="TaskScheduler.Pages.CreateTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Создание задачи"
      Background="#F4F6F6">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Width="450" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0, 50, 0, 50">
            <TextBlock Text="Создание новой задачи" FontSize="32" FontFamily="Segoe UI" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <TextBlock Text="Заголовок" FontSize="16" Margin="0,0,0,5"/>
            <TextBox x:Name="tbTitle" Height="35" FontSize="16" Padding="5" Margin="0,0,0,20"/>
            <TextBlock Text="Описание" FontSize="16" Margin="0,0,0,5"/>
            <TextBox x:Name="tbDescription" Height="100" TextWrapping="Wrap" AcceptsReturn="True" FontSize="16" Padding="5" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                <StackPanel Width="200">
                    <TextBlock Text="Срок выполнения" FontSize="16" Margin="0,0,0,5"/>
                    <DatePicker x:Name="dpDueDate" FontSize="16" Height="35"/>
                </StackPanel>
                <StackPanel Width="200" Margin="50,0,0,0">
                    <TextBlock Text="Приоритет" FontSize="16" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cbPriority" FontSize="16" Height="35" SelectedIndex="0">
                        <ComboBoxItem Content="Низкий"/>
                        <ComboBoxItem Content="Средний"/>
                        <ComboBoxItem Content="Высокий"/>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
            <Separator Margin="0, 10, 0, 20" Background="#BBBBBB"/>
            <TextBlock Text="Дополнительные параметры" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Тип задачи" FontSize="16" Margin="0,0,0,5"/>
                <ComboBox x:Name="cbTaskType" FontSize="16" Height="35" SelectedIndex="0">
                    <ComboBoxItem Content="Обычная"/>
                    <ComboBoxItem Content="Проект"/>
                    <ComboBoxItem Content="Личная"/>
                    <ComboBoxItem Content="Встречи"/>
                </ComboBox>
            </StackPanel>
            <TextBlock Text="Повторяемость" FontSize="16" Margin="0,0,0,5"/>
            <ComboBox x:Name="cbRecurrence" FontSize="16" Height="35" SelectedIndex="0" Margin="0,0,0,30">
                <ComboBoxItem Content="Нет (Однократно)"/>
                <ComboBoxItem Content="Ежедневно"/>
                <ComboBoxItem Content="Еженедельно"/>
                <ComboBoxItem Content="Ежемесячно"/>
                <ComboBoxItem Content="Ежегодно"/>
            </ComboBox>
            <Button x:Name="btnSaveTask" Content="Сохранить задачу" Height="40" Width="250" HorizontalAlignment="Center" Click="btnSaveTask_Click" Style="{StaticResource LightBlueButton}"/>
        </StackPanel>
    </ScrollViewer>
</Page>