<UserControl x:Class="TaskScheduler.UserControls.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskScheduler.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColSideBar" Width="200"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#34495E">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <StackPanel Orientation="Horizontal" Margin="10,15,0,25">
                        <Image Source="/Assets/logo.png" Width="25" Height="25" Stretch="Uniform"/>
                        <TextBlock Text="Меню" Foreground="White" FontSize="18" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="btnMyTasks" Click="btnMyTasks_Click" Tag="0" Content="Все задачи" Style="{StaticResource SidebarButton}"/>
                    <Button x:Name="btnBase" Click="btnMyTasks_Click" Tag="1" Content="Обычные" Style="{StaticResource SidebarButton}"/>
                    <Button x:Name="btnProject" Click="btnMyTasks_Click" Tag="2" Content="Проекты" Style="{StaticResource SidebarButton}"/>
                    <Button x:Name="btnPersonal" Click="btnMyTasks_Click" Tag="3" Content="Личные" Style="{StaticResource SidebarButton}"/>
                    <Button x:Name="btnMeetings" Click="btnMyTasks_Click" Tag="4" Content="Встречи" Style="{StaticResource SidebarButton}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,15,0,10">
                    <Button x:Name="btnCreateTask" Click="btnCreateTask_Click" Content="Создать задачу" Style="{StaticResource SidebarButton}"/>
                    <Separator Height="1" Background="#5A7490" Margin="10,10,10,10"/>
                    <Button x:Name="btnExit" Click="btnExit_Click" Content="Выйти" Style="{StaticResource SidebarButton}"/>
                </StackPanel>

            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" 
                      Width="5" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Stretch"
                      Background="#BBBBBB" 
                      ResizeDirection="Columns"/>

        <Border Grid.Column="2" Background="#F4F6F6">
            <Frame x:Name="MainContentFrame" 
                   NavigationUIVisibility="Hidden"/>
        </Border>
        
    </Grid>
</UserControl>